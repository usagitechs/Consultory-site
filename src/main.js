/**
 * usagiteks - Corporate website entry point.
 * Modules live in src/<feature>/index.js and are wired by src/app.js.
 */
import app from './app.js'

document.addEventListener('DOMContentLoaded', app);
